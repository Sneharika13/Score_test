Base page: its having all dependent and reusable methods
Dashboard: its having all objects related to main page and followed page factory approach

Main Execution:
TestSuite.xml file we need to trigger by passing the platform as parameter as of now we are passing Android as platform